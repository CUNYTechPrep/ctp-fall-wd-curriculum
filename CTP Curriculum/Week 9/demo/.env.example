# Environment
NODE_ENV=development

# AWS Configuration (from Week 8)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test

# Cognito Configuration (from Week 8)
NEXT_PUBLIC_COGNITO_USER_POOL_ID=your-user-pool-id
NEXT_PUBLIC_COGNITO_CLIENT_ID=your-client-id
COGNITO_ENDPOINT=http://localhost:4566

# DynamoDB Configuration (from Week 8)
DYNAMODB_ENDPOINT=http://localhost:4566
DYNAMODB_TABLE_NAME=UserSettings

# S3 Configuration (from Week 8)
S3_ENDPOINT=http://localhost:4566
S3_BUCKET_NAME=profile-photos
NEXT_PUBLIC_S3_URL=http://localhost:4566

# PostgreSQL Configuration
# Local Development (Docker PostgreSQL - same as RDS, just local)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/ctpdb
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=ctpdb
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# AWS RDS PostgreSQL (Production)
# The DATABASE_URL is the ONLY thing that changes from local to RDS!
# Everything else (SQL, Drizzle, queries) stays the same.
# 
# Example RDS connection string:
# DATABASE_URL=postgresql://dbadmin:yourpassword@mydb.abc123.us-east-1.rds.amazonaws.com:5432/ctpdb
#
# RDS Best Practices:
# - Store in AWS Secrets Manager or Parameter Store
# - Use IAM authentication instead of passwords when possible
# - Never commit real credentials to git!

# RDS Configuration (for production deployment)
# DATABASE_URL=postgresql://username:password@your-rds-endpoint:5432/dbname
